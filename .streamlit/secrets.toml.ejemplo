# Copia este archivo como  secrets.toml  (sin ".ejemplo") para probar en LOCAL.
# En Streamlit Community Cloud NO subas este archivo: la contraseña se pone en
# Settings -> Secrets de la app (mismo formato).

app_password = "cambia-esta-contrasena"
